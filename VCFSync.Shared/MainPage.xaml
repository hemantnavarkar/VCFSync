<Page
    x:Class="VCFSync.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VCFSync"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
      <TextBlock Grid.Row="0" 
                 Text="VCF Sync" 
                 Margin="10" 
                 HorizontalAlignment="Center" 
                 FontSize="25" 
                 Foreground="Black"
                 />

        <Panel Grid.Row="1"/>

        <WrapPanel Grid.Row="2" Margin="10,10,10,10" HorizontalAlignment="Center">
            <Button Content="Import" Margin="5" Click="TestClick"></Button>
            <Button Content="Export" Click="TestClick"></Button>
        </WrapPanel>
    </Grid>
</Page>
